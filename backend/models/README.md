# Models folder

Put your trained model files here, for example:

- `handwriting.pkl` or `handwriting.pt` — handwriting risk model
- `speech.pt` or `speech.h5` — speech risk model
- `cognitive.pkl` — cognitive (reaction time) risk model

In `main.py`, load them at startup or inside the stub functions, then replace the stub return values with real predictions.
